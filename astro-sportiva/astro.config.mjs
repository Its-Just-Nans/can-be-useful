import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
    site: "https://n4n5.dev",
    base: "/can-be-useful/astro-sportiva/",
    trailingSlash: "always"
});
