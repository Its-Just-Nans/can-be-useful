# Amélioration de la machine

Les mojesi ont une idée pour améliorer le fonctionnement de leurs machines. Ils décident d'en trafiquer une et de lui brancher un petit appareil contenant un bouton. Ce petit appareil permet d'aller placer un curseur dans le texte affiché, afin de pouvoir le modifier à n'importe quel endroit de la page.

Votre éditeur de texte s'enrichit donc avec l'instruction click, qui permet de gérer un curseur.

L'événement click suivi de ses coordonnées x et y déplacera le curseur dans la zone de texte (x pour sa position dans la ligne et y pour le numéro de ligne). Le texte reçu ensuite partira de ce curseur et décalera le reste du texte.

Input de l'événement click :

```txt
click
0
2
```

où 0 correspond à la coordonnée x et 2 correspond à la coordonnée y.

Un événement backspace reçu après un click effacera le caractère précédant le nouveau curseur.

Remarque : Si vous souhaitez récupérer le code de l'exercice précédent, cliquer sur le bouton 🕐 au dessus de la console de développement.

Exemple 1 - Gestion d'un curseur sur une ligne
Paramètre d'entrée

```txt
10
12
p
i
n
e
p
p
l
e
click
4
0
a
```

Sortie attendue

```txt
pineapple
```

Exemple 2 - Gestion d'un curseur sur deux lignes

Paramètre d'entrée

```txt
4
11
m
a
n
y
r
w
s
click
1
1
o
```

Sortie attendue

```txt
many
rows
```
